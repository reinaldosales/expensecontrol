@{
    ViewData["Title"] = "Home Page";
}

<div class="text-center">
    <h1 class="display-4">Bem vindo!</h1>

    @if(User.Identity.IsAuthenticated)
    {
        <p>
            <b>@User.Identity.Name</b>, aqui você poderá gerenciar seus controles de despesas.
        </p>
    }
</div>
